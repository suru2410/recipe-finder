import React from "react";
import { Typography, Box } from "@mui/material";

const SearchList = () => (
  <Box sx={{ p: 4, textAlign: "center" }}>
    <Typography variant="h5">
      Search results are now handled dynamically on the Home page.
    </Typography>
  </Box>
);

export default SearchList;
